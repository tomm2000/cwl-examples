version: v1.0
workflows:
  master:
    type: cwl
    config:
      file: main.cwl
      settings: inputs.yml
    bindings:
      - step: /write
        target:
          deployment: local
      - step: /count
        target:
          deployment: local

deployments:
  local:
    type: local
    config: {}
